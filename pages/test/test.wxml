<!--pages/test/test.wxml-->
<view wx:for="{{array}}">
  <view bindtap='itemClick' data-birthday='{{item.birthday}}' data-id='{{item.id}}'>{{item.name}}</view>
</view>

<button class='button' type="primary" bindtap='click'>点我</button>
